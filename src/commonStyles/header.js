import styled from "styled-components"

export const HeaderWrapper = styled.header`
  font-size: 2rem;
  padding: 1.6rem 6rem;
  margin: 0 20rem;
  background: #1f2640;
  img {
    width: 14rem;
  }
`
